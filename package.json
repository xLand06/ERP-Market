{
  "name": "erp-market",
  "version": "1.0.0",
  "description": "Un moderno sistema ERP diseñado específicamente para bodegones y minimarkets. Facilita la gestión integral del negocio: desde el ingreso de productos y seguimiento Kanban, hasta el control de inventario, finanzas multimoneda y cálculo de comisiones.",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --parallel --filter './backend' --filter './frontend' dev",
    "dev:desktop": "pnpm --parallel --filter './backend' --filter './frontend' --filter './desktop' dev",
    "postinstall": "pnpm --filter './backend' postinstall",
    "setup": "pnpm --filter './backend' db:setup:local",
    "build": "pnpm --filter './backend' --filter './frontend' build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xLand06/ERP-Market.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/xLand06/ERP-Market/issues"
  },
  "homepage": "https://github.com/xLand06/ERP-Market#readme",
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "better-sqlite3",
      "prisma",
      "@prisma/engines",
      "esbuild"
    ]
  }
}